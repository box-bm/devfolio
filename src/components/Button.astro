---
interface Props {
  type?: "Outlined" | "Default" | "Text" | "Icon";
  target?: "_self" | "_blank" | "_parent" | "_top";
  color?: string;
  id?: string;
  href?: string;
  ariaLabel?: string;
  magnetic?: boolean;
}

const {
  type = "Default",
  href,
  target = "_blank",
  color,
  id,
  ariaLabel,
  magnetic = false,
} = Astro.props;
---

<a
  id={id}
  target={target}
  href={href}
  style={color ? `color: ${color}` : undefined}
  aria-label={ariaLabel}
  data-magnetic={magnetic ? "0.25" : undefined}
  class:list={[
    "btn",
    type === "Default" && "btn-default",
    type === "Outlined" && "btn-outlined",
    type === "Text" && "btn-text",
    type === "Icon" && "btn-icon",
  ]}
>
  <span class="btn__label"><slot /></span>
</a>

<style>
  .btn {
    position: relative;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    padding: 13px 22px;
    border-radius: var(--radius);
    font-weight: 500;
    font-size: 0.9rem;
    line-height: 1;
    cursor: pointer;
    overflow: hidden;
    isolation: isolate;
    transition:
      transform var(--dur-fast) var(--ease-out),
      box-shadow var(--dur-fast) var(--ease-out),
      background-color var(--dur-fast) var(--ease-out),
      border-color var(--dur-fast) var(--ease-out),
      color var(--dur-fast) var(--ease-out);
  }
  .btn__label {
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  /* shine sweep */
  .btn::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background: linear-gradient(
      110deg,
      transparent 30%,
      rgba(255, 255, 255, 0.35) 50%,
      transparent 70%
    );
    transform: translateX(-120%);
    transition: transform 650ms var(--ease-out);
  }
  .btn:hover::before {
    transform: translateX(120%);
  }

  .btn:active {
    transform: scale(0.96);
  }

  /* Default — flat sand fill */
  .btn-default {
    color: var(--on-accent);
    background: var(--olive, var(--accent));
    border: 1px solid var(--olive, var(--accent));
  }
  .btn-default:hover {
    background: var(--olive-hover, var(--accent-strong));
    border-color: var(--olive-hover, var(--accent-strong));
    transform: translateY(-1px);
    box-shadow: var(--shadow-accent);
  }

  /* Outlined — acrylic hairline */
  .btn-outlined {
    color: var(--text-primary);
    background: var(--glass-bg);
    backdrop-filter: blur(var(--glass-blur));
    -webkit-backdrop-filter: blur(var(--glass-blur));
    border: 1px solid var(--glass-border);
  }
  .btn-outlined:hover {
    transform: translateY(-1px);
    border-color: var(--accent);
  }

  /* Text */
  .btn-text {
    background: transparent;
    color: var(--accent);
    padding-inline: 14px;
  }
  .btn-text:hover {
    background: var(--accent-soft);
  }
  .btn-text::before {
    display: none;
  }

  /* Icon — hairline square */
  .btn-icon {
    width: 34px;
    height: 34px;
    padding: 0;
    border-radius: var(--radius-sm);
    color: var(--text-secondary);
    background: transparent;
    border: 1px solid var(--glass-border);
  }
  .btn-icon::before {
    display: none;
  }
  .btn-icon:hover {
    color: var(--text-primary);
    border-color: var(--accent);
  }
</style>
