---
import Button from "./Button.astro";
import { Link, Github } from "@lucide/astro";

interface Props {
  title: string;
  description: string;
  viewLink: string;
  codeLink: string;
}

const { title, description, viewLink, codeLink } = Astro.props;
---

<div class="project-card" data-card>
  <div class="project-card-content">
    <h4>{title}</h4>
    <p>
      {description}
    </p>
    <slot name="content" />
  </div>
  <div class="project-card-tech">
    <slot name="tech" />
  </div>
  <div class="project-card-actions">
    <Button type="Outlined" href={codeLink}><Github />Code</Button>
    <Button href={viewLink}><Link />View</Button>
  </div>
</div>

<style>
  .project-card {
    display: flex;
    flex-direction: column;
    border-radius: 12px;
    border: 1px solid var(--neutral-medium);
    border-style: solid;
    border-width: 1px;
    padding: 20px;
    gap: 8px;
    transition: all ease 250ms;
    cursor: pointer;
  }

  .project-card:hover {
    border-color: var(--neutral-dark);
    background-color: var(--neutral-light);
  }

  .project-card-content {
    display: flex;
    flex-direction: column;
    height: 100%;
    gap: 4px;
    margin-bottom: 8px;
  }

  .project-card-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
  }

  .project-card-actions {
    display: flex;
    gap: 8px;
    flex-direction: row;
  }

  .project-card-actions a {
    width: 100%;
  }
</style>

<script define:vars={{ viewLink }}>
  const cards = document.querySelectorAll("[data-card]");

  cards.forEach((card) => {
    const buttons = card.querySelectorAll("a, button");

    card.addEventListener("click", () => {
      window.open(viewLink, "_blank");
    });

    buttons.forEach((btn) => {
      btn.addEventListener("click", (e) => {
        e.stopPropagation();
      });
    });
  });
</script>
